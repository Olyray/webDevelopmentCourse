<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice">🍗</button>
    <button type="submit" value="beef" name="choice">🥩</button>
    <button type="submit" value="fish" name="choice">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">
    <% if (locals.chosen) {%>
    <!-- If no recipe yet, show: -->
      <h2><%= chosen.charAt(0).toUpperCase() + chosen.slice(1) %> Taco</h2>
      <h3>Ingredients:</h3>
      <% chosenRecipe = recipeData.find(chosenOne => chosenOne.name === `${chosen.charAt(0).toUpperCase() + chosen.slice(1)} Taco` ) %>
      <ul id="ingredientsList">
        <li><%= chosenRecipe.name %>, <%= chosenRecipe.ingredients.protein.preparation %></li>
        <li><%= chosenRecipe.ingredients.salsa.name %></li>
        <% for (let i = 0; i < chosenRecipe.ingredients.toppings.length; i++) {%>
        <li><%= chosenRecipe.ingredients.toppings[i].quantity + "of" + chosenRecipe.ingredients.toppings[i].name %></li>
        <% } %>
      </ul>
    <% } else {%>
      <h2>Pick your favourite taco ingredient 👆</h2>
    <% } %>
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->





  </div>
</body>

</html>